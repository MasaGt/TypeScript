### 結論

- tsc `--build` オプションをつければ、 `--clean` オプションをつけ、前回のビルド結果の削除を実行できる

    - しかし、 `tsc --build --clean` は**前回のビルド結果の削除を行うコマンド**であることに注意 (その後のコンパイルは別途自分で行う)

<br>

- tsc <オプション> で 「前回のビルド結果の削除」 → 「コンパイル」 を行ってくれるオプションは無い